interface:
  display_name: "Software Engineering"
  short_description: "Choose direct work, delegation, or a coordinated team"
  default_prompt: "Use $software-engineering to define the contract, choose direct work or bounded delegation, and invoke conditional Teamwork only when a separate orchestrator adds value; verify the integrated result."
